{% extends "base" %}

{% block content %}
    <h1>Hello, {{ name }}!</h1>
    {% for post in posts %}
        <div>
            <p>{{ name }} says: {{ post.body }}</p>
        </div>
    {% endfor %}
{% endblock content %}

